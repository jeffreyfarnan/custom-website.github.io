$("document").ready(function() {
    // Tells the browser to open the link page in a new window
        //$("a").attr("target", "_blank");

    // Remove an attribute, ie the href link
       //$("a").removeAttr("href");
        
    // Change the source of the image to a Spring image  
       $("img").attr({ src: "images/admin.jpg", alt: "spring" });
    });
    
    